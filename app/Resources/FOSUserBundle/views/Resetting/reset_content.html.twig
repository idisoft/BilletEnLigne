{% trans_default_domain 'FOSUserBundle' %}
{% form_theme form 'TechniGestMainBundle:MesVues:formTheme.html.twig' %}

<div class="mywell">
    <form action="{{ path('fos_user_resetting_reset', {'token': token}) }}" method="POST" class="fos_user_resetting_reset">
        {{ form_widget(form) }}
        <center>
            <input type="submit" value="{{ 'resetting.reset.submit'|trans }}" class="btn btn-primary btn-sm" />
        </center>
    </form>
</div>

